package com.example.isdb.enums

/**
 * Simple enum class to reduce dependency on hard-coded strings.
 * Used for distinguishing type of activity i.e., isdb or register
 */
enum class Log {
  LOGIN,
  REGISTER
}